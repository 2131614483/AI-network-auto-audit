# 全量回归 · 结果小结

**执行时间**：2026-09-12 11:29 → 12:19（**50 分 06 秒**）
**命令**：`.venv\Scripts\python.exe -m pytest -q --durations=25 --junitxml=全量回归-junit.xml`

## 结果

| 项 | 值 |
| --- | --- |
| **通过** | **1213** |
| **跳过** | **9** |
| **失败** | **0** |
| 用例总数 | 1222 |
| 涉及文件 | 174 |

## 9 个跳过的原因（全部是环境门控，非失败）

| 数量 | 原因 |
| --- | --- |
| 3 | 本地 Ollama 服务不可用（`test_cw2_full_log_ai.py`）—— 采集时刻 Ollama 未运行，见 `07-环境与版本指纹/ollama-models.json` |
| 1 | 需要 `AUDIT_NETWORK_RUN_OLLAMA_TESTS=1` 才跑本地模型冒烟（慢，真实推理） |
| 1 | 隔离演练已记录，需重建测试库后重跑 |
| 2 | 需要 `AUDIT_NETWORK_RUN_MINERU_TESTS=1` 才跑真实 MinerU 集成 |
| 2 | 外部 worker 抢先消费了共享测试队列 |

## 与上一轮的差异

| | 上一轮 | 本轮 | 说明 |
| --- | --- | --- | --- |
| 通过 | 1215 | 1213 | **-2** |
| 跳过 | 6 | 9 | **+3** |
| 失败 | 0 | 0 | — |

差异**全部来自 Ollama 未运行**：3 个原本会真实调用本地模型的用例转为 skip（passed→skipped）。
用例总数、失败数均未变化。这不是回归。

## 本目录文件

| 文件 | 内容 |
| --- | --- |
| `全量回归-完整输出.log` | pytest 完整输出（含最慢 25 个用例的耗时分解） |
| `全量回归-junit.xml` | 机器可读的逐用例结果（含每个用例的状态与耗时） |
| `逐用例清单.md` | 人类可读版：按状态分组 + 通过用例按文件归组统计 |
| `逐用例清单.csv` | 同上，Excel 可直接打开 |
