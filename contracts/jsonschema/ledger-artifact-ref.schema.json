{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://audit.local/contracts/ledger-artifact-ref.schema.json",
  "title": "LedgerArtifactRef",
  "description": "已校验总账 CSV 工件引用：不可变哈希、期间与列映射版本；内容不嵌入控制面消息。",
  "type": "object",
  "additionalProperties": false,
  "required": ["contract_id", "contract_version", "artifact", "schema_mapping_version", "period"],
  "properties": {
    "contract_id": {"const": "ledger-artifact-ref"},
    "contract_version": {"const": "1.0.0"},
    "artifact": {"$ref": "artifact-ref.schema.json"},
    "schema_mapping_version": {"type": "string", "pattern": "^(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)\\.(0|[1-9][0-9]*)$"},
    "period": {"type": "string", "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])$"},
    "amount_threshold": {"type": "number", "minimum": 0, "maximum": 1e15}
  }
}