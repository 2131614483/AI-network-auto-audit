# 2026-09-11 工作日志

## AI 组网 Demo 全链路验证（产出 `docs/AI组网Demo-全链路功能验证报告-20260911.md`）

验证对象：`scripts/demo-grouping-business.py`（8 步全链路：模拟数据 → 能力召回 → 云端 AI 组网 → 编译闸门 → Policy 裁决 → 真实子进程执行 → 画布投影 → 证据导出 → 三层下钻）。

方法：源码通读 + 实机重跑 + 独立复算（不复用项目代码）+ 隔离变量 A/B/C 对照实验。

### 验证通过的部分
- 质检 6 项候选：自行实现 6 条规则重算，与插件产物逐条一致。
- CSV sha256 `1f8e4bfd…`、548B、9 行，复算一致。
- 上游产物哈希：`efa1f357965be17d…`(candidates)、`c620e0e21184e6d4…`(evaluation)、`6d42d4e630509d52…`(backtest，且等于 evaluation 里的 report_sha256)。
- 6 个历史证据包 `verify_evidence_bundle` 全部 ok=True / checked=5。
- `ruff check .` 全绿；`mypy packages` 101 文件 0 问题。
- AI 组网测试子集 8 文件：98 passed / 1 failed / 1 skipped。

### 查实的缺陷（已写入报告 §7/§8）
1. **D1 P0**：demo 的 seed 绑定硬编码 `("ledger-a","ledger")`（`demo-grouping-business.py:259`），而提示词要求 AI 用语义化节点名（`ledger-validate-001`）。A/B/C 对照证明引擎正确、缺陷只在脚本。本轮云端 AI **首次真的通过编译闸门**（`draft_ready, revisions=3`），因此旧报告"云端从未通过闸门"的结论已过期，且 demo 的"AI 成功路径"从未被验证过。
2. **D2 P0**：`ports_executor.py:271` 的 `_bind_inputs` 在 try 之外，上游失败后被下游未捕获异常中断 → 下游 attempt 永久停在 `running`、`execution_runs` 无 run 记录；测试库共 5 条滞留 running。
3. **D3 P0**：`plan_key` / `node_instance_id` 未做字符集校验即拼入落盘路径（`ports_executor.py:175`），实测 `plan-../../../../pwned` 可编译并逃出 staging 根。
4. **D4 P1**：`port_adapters.py:30` 取 `quality_hash`（契约里没有）→ 恒为 `ledger-quality:unknown`，应改取 `rule_pack_sha256`。
5. **D5 P1**：demo 诊断查询未设 `app.tenant_id`，RLS 下返回 0，导致旧报告"blueprints 命中 0 行"的错误结论（实际 739 行）。
6. **D8 P1**：测试库策略集污染 → `test_api_planning_fail_closed_without_capability` 假失败（8 个 active 集合授权 `topology.intent.plan`，用例只停用 1 个；`_load_policy` 是跨集合加性的）。
7. **D9 P2**：全量 pytest 跑 35 分 30 秒未结束（无 sleep，疑与库污染有关），本次未取得全量数字。
8. **安全**：`tests/integration/test_cw5_second_llm_adapter.py:318` 含**明文真实 API Key**（92 字符，与环境变量逐字符一致）。仓库当前**零提交**、无 remote，故未进 git 历史。建议轮换。

### 关键环境事实（可复用）
- 测试库：`postgresql://audit_app:admin@localhost:5432/audit_network_test`，租户 `local-dev` = `a5526a1e-02ed-4faa-a663-5413b7c3db88`。
- PG 16.13 本机原生运行；`plugin_blueprints` 设租户后 739 行、`policy.policy_sets` 3329 行（1582 active）——测试库污染严重。
- **Ollama 已安装**（含 `qwen3_27b_iq3xxs_64k:latest`），旧文档"本机未安装 Ollama"已过期；但 `scripts/evaluate-ai-planning.py` 硬编码走 Ollama，未支持云端 backend。
- 新增对照实验脚本：`.data/demo/_verify_semantic_seed.py`（只写 `.data/demo`）。

## 项目文档化（产出 `docs/项目说明-审计智能中枢.md`、`docs/简历与面试话术-审计智能中枢.md`）

背景：用户需要「项目文档 + 简历项目内容 + 面试话术」三件套（求职方向：**四大 / 银行 / 券商 / 投行 / 国企**，上海-苏南；用户已明确**不去禾赛科技**，早期版本中的禾赛相关内容已全部移除）。

简历版本切分（改版后）：版本 A 审计/财务/内审（四大、银行与国企内审）、版本 B 银行/券商/投行/国企方向（合规风控·数据治理·私有化部署）、版本 C 精简版、版本 D 一行版；另加「面向不同雇主的讲法切换」对照表。
- **面试钩子沉淀**：本项目的"审计域↔量化域信息防火墙"正好对应券商/投行的**信息隔离墙（Information Barrier）**制度，是投行/券商向的最高价值切入点；银行向主打多租户 FORCE RLS + 最小授权 + 不可篡改账本 + 数据不出域。

## 补充：知识图谱驱动组网（用户提出的重点，已写入文档）

用户认为**知识图谱的组网价值必须讲出来** —— 它可以提升 AI 组网的准确度、成功率和效率。已核实并写入：

- **事实核查结论（关键，勿写错）**：`packages/ai_planner/`（planner/catalog/composer）**完全不读取** `packages/experience/` 与图谱 —— grep `experience|edge_stats|node_stats|read_overlay` 在 ai_planner 下**零命中**。当前组网输入只有四样：能力目录（DB 蓝图 ∩ 运行时）＋ 关键词召回（`composer.recall_by_keywords`，匹配 name/description/capability）＋ 契约相等连线（`schema_ref`）＋ **静态硬编码模板**（`packages/ai_planner/templates.py` 的 `TEMPLATES` / `template_block`）。
- 因此正确表述是：**经验态图层已完整落地（0055–0057），但只服务于知识星云可视化与人工决策；回流组网是明确的下一步，不是已完成项。** 设计稿 §3.2 的 L0 定义本身就写明"只改变边的粗细/颜色/排序，不改关系集合、不授权、不执行"。
- 已写入 `docs/项目说明-审计智能中枢.md` **§5.10「知识图谱驱动组网」**：双图层现状表（设计态/经验态）、经验闭环 7 步、L0/L1/L2 三级分级、缺口分析（四样组网输入）、**三条回流通道**（召回排序／连线先验／失败规避）+ 模板动态化、安全边界（Wilson 下界 ≥ 阈值且样本量 ≥ N、仅改概率分布不改权限集合、不得跨租户污染先验）、验收指标（首次通过闸门率／平均修订轮数／端到端时延／候选 token 量）与现成脚手架。
- 简历侧：版本 A 加「审计经验沉淀」条、版本 B 加「知识图谱驱动组网」条（标 ★）；话术侧新增 **STAR 5** 与 **Q14**；速记卡加「一个闭环」；讲法切换表为四类雇主各加了经验沉淀角度。
- **可复用的讲法金句**：「经验优化的是**概率分布**，不是**权限边界**」—— 这句话同时满足了"讲高级机制"和"不越 Phase 9 安全边界"两个要求，是本项目所有增强提案的通用话术模板。

摸清的项目事实（本次实测，可复用）：
- 后端 `packages/` **107 个源文件 / 45,785 行**；`apps/api/main.py` 6747 行、**112 个端点**（GET 62 / POST 49 / PUT 1）。
- Alembic **60 个迁移**（0001→0058）；**16 个 schema / 113 张表**；29 张核心表 FORCE RLS、33 条策略；`audit_migrator` / `audit_app` 分级角色，应用角色无 DELETE/DDL/BYPASSRLS。
- `require_policy` 在 API 层调用 **105 处**；策略账本 `policy.tool_calls`/`decisions` 仅授 INSERT。
- 测试 **177 个文件 / 33,068 行**（unit 67/641、integration 72/361、contract 33/147、e2e 3/3、evaluation 1/5、migration 1/3）；最近全量 **1165 passed / 6 skipped / 1 failed**；mypy strict 107 文件 0 问题。
- 桌面端 Electron **37** + React **19** + AntD **5**；13 个视图；vitest **73 passed**（8 文件）。
- 契约 `contracts/jsonschema/` **86 份** Draft 2020-12，UPP v1.0；插件 `plugins/builtin/` 归一化去重 **132 个**，其中 `audit-*` 契约目录 **103 个**（实测：foundation 18 / field 14 / risk 11 / evidence 11 / mandate 10 / plan 9 / finding 9 / report 8 / remedy 7 / govern 6 / 其余 4）。
  - ⚠️ 官方"100 插件 = 支撑 18 + 核心业务 76 + 治理 6"是**阶段验收口径**，与当前目录实测的 103 个 `audit-*` 有差异（验收后有增补）。写文档/简历时以"100 个已验收"表述，并标注实测口径，避免被面试官追问时对不上。
- 仓库**零 git 提交**（`git log` 返回 0），本地开发未纳入版本控制。

文档写法约定（本次沉淀，后续沿用）：
- 简历描述按投递方向分四版（审计/财务向、数据技术向、精简版、一行版）+ 一张「量化数据速查表」。
- 话术部分：30 秒电梯 → 90 秒主线（问题/取舍/方案/结果） → 4 个 STAR（闸门设计 / 证据链可复算 / 死循环修复 / 自我对抗验证） → 12 条高频追问 → 陷阱题（含"是不是 AI 帮你写的"） → 反问清单 → 速记卡。
- **数字必须可追溯**：凡写进文档/简历的数字都要能从仓库实测得到，口径不一致处如实标注，不为了好看凑整。
