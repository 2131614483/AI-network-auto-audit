# 2026-09-04 工作日志

## Phase 2 知识输入与文档生命周期 —— 验收完成

- 盘点现状：Phase 2 的后端（`packages/knowledge/` 的 ingest/library/retrieval/local_extractors）、API（`apps/api/main.py` 的 upload/retire/restore/recycle-bin/adapters）、桌面 GUI（`desktop/src/App.tsx` + `desktop/electron/main.ts`）与测试已基本就位。
- 发现并闭合唯一明确缺口：`upload`/`retire`/`restore` 三写操作的幂等键头此前「接收但忽略」。修复方式（与 `invoke_verified_plugin` 一致）：
  - `require_policy` 新增 `idempotency_key` 参数并透传给 `evaluate_policy`（后者已完整实现幂等记录：复用键+同载荷返回缓存决策，复用键+不同载荷 409）。
  - `knowledge_document_retire/restore` 把 `_idempotency_key` 改名 `idempotency_key` 并真正接入。
  - `knowledge_upload` 新增 `Idempotency-Key` 头，并把「文件名 + relative_paths」指纹绑定进策略参数 `upload_fingerprint`。
- 新增回归测试：`tests/integration/test_knowledge_document_lifecycle_api.py` 增加 retire 复用键 409、upload 复用键 409 两个用例；`_allow_lifecycle` 补充 `knowledge.upload` 允许。
- 全量回归：Python 93 passed、Ruff 通过、Mypy 25 源通过、桌面 typecheck 通过、Vitest 5/5、生产构建通过。
- 更新 `docs/status.md` 与 `docs/phase2-knowledge-library-plan.md`，标记 Phase 2 完成。

## 环境坑（本轮沉淀）

- **沙箱安全删除 shim** 拦截回收站删除：`mypy` 清 `.mypy_cache/missing_stubs`、`electron-vite build` 清 `desktop/out/` 都会失败。
  - 规避：`mypy --no-incremental`；桌面构建前先把 `desktop/out` `mv` 成临时名（重命名不受拦截），构建后再 `rm -rf` 临时名（需放行沙箱）。
- 运行环境：Python 用 `C:\ProgramData\Anaconda3\python.exe`（3.13.9，含 pytest/ruff/mypy）；测试库连接 `postgresql://audit_app:admin@localhost:5432/audit_network`。

## Phase 3 本地富媒体解析（MinerU）—— 冒烟验证完成

- 用户选定方向 A，并告知 MinerU 源码在 `D:\MinerU-master`，**只读参考、绝不修改**。
- 探查结论（只读）：
  - 入口 `D:\MinerU-master\.venv\Scripts\mineru.exe`（venv 已装 mineru+torch+pypdfium2），CLI 定义在 `mineru/cli/client.py`，`mineru = mineru.cli.client:main`。
  - 命令契约：`mineru -p <文件> -o <输出> -b pipeline -m auto|txt|ocr -l ch`，默认 `hybrid-engine`。
  - 模型已缓存 `~/.cache/huggingface` + `~/.cache/modelscope`；本机 GPU 16GB。
- 冒烟实测成功：`demo1.pdf`（13 页）`-b pipeline -m txt -f false -t false` 约 19 秒跑通，输出到 `audit_network/.data/mineru-smoke/`（未写 MinerU-master）。
  - 输出：`<out>/<stem>/<method>/` 含 `<stem>.md`、`<stem>_content_list.json`（`{type,text,bbox,page_idx,text_level}`，页码回链依据）、`<stem>_middle.json`、`images/*.jpg`。
- 已写 `docs/phase3-rich-media-plan.md`（契约 + 目标 + 安全边界 + 验收）。下一步实现 `packages/knowledge/rich_media.py` 的 MineruAdapter（子进程隔离 + SHA256 绑定 + 输出解析），登记 `local.mineru` 运行时绑定，接入 Policy Gateway + ChangeSet。
- 注意：MinerU 单文件约 19 秒，批量需控制并发/超时；扫描件用 `-m ocr` 更慢，带文本层用 `-m txt` 最快。

## Phase 3 适配器实现（MineruAdapter）—— 已完成

- 新增契约 `contracts/jsonschema/rich-media-parse-result.schema.json`（ParsedDocument：markdown + 带 page_idx/bbox/text_level 的 chunks）。
- 新增 `packages/knowledge/rich_media.py`：
  - `MineruAdapter.extract(uri, sha256, size_bytes)`：`_resolve_input`（文件 URI 解析 + 声明根边界 + 后缀白名单 + SHA256/大小校验）→ `subprocess.run([mineru.exe, -p, -o, -b pipeline, -m, -l])`（隔离工作目录 + 超时 + 白名单环境变量 `_MINERU_ENV_KEYS`，不 `os.environ.copy()`）→ `parse_mineru_output`（读 `<stem>.md` + `<stem>_content_list.json` → `ParsedDocument`，经 `validate_parse_result` 校验）。
  - `MINERU_EXECUTABLE` 默认 `D:\MinerU-master\.venv\Scripts\mineru.exe`，可用 env 覆盖；MinerU 源码只读、输出只落 `.data/`。
- 测试：`tests/unit/test_rich_media.py`（7 项）、`tests/integration/test_rich_media_mineru.py`（opt-in，`AUDIT_NETWORK_RUN_MINERU_TESTS=1` 启用，用 `_minimal_pdf()` 手写极简 PDF，实测 33.5s 通过；默认跳过）。
- 回归：pytest 100 passed + 1 skipped、Ruff、Mypy 26 源通过。
- 关键坑：`content_list.json` 的 `page_idx` 是 0 基；bbox 是 4 元素 `[x0,y0,x1,y1]`；image/table 块无 `text` 字段（落在 `img_path`/`table_body`）。
- 待续（Phase 3 剩余）：`local.mineru` 运行时登记、策略 capability + Policy Gateway、ChangeSet 写 `semantic.chunks`（带页码 metadata）、API 端点 + 桌面入口。

## Phase 3 服务层 + 策略网关 + API —— 已完成

- 新增 `packages/knowledge/rich_media_service.py`：`extract_deferred_rich_media`（读待处理 ingest 文件 → 释放 DB 锁跑 MinerU 子进程 → 写 `semantic.chunks` 带页码 metadata → 标记 completed）。关键点：
  - `_storage_path` 用「文本剥离 `file://` 前缀」还原 Windows 路径（`ingest.py` 存 `f"file://{path}"` 会得到 `file://C:\...`，`urlparse` 会把 `C:` 当 netloc，不能用）。
  - `register_uuid()` 必须调用（否则 `can't adapt type 'UUID'`）；已加。
  - 长事务持锁问题：MinerU 子进程约 30s，不能在 `FOR UPDATE` 事务里跑；拆成「读 → 跑子进程 → 写回」两段短事务，末尾 `UPDATE ... WHERE status='waiting_extractor'` 防并发重复处理。
- 更新 `local_extractors.py`：`MINERU_EXECUTABLE` 常量下沉（`mineru_available()`），`local_adapter_catalog()` 如实反映 `local.mineru` 可用性（`available`/`isolated_subprocess`）；`rich_media.py` 改为从此导入，避免循环依赖。
- API 端点 `POST /api/v1/knowledge/rich-media/extract`（capability `knowledge.extract.rich_media`，high/write_data，幂等键）→ `MineruAdapter` → 服务层。`rich_media_read_roots` 返回 drop 根 + 已登记只读源根。
- `registration.py` 新增 `publish_rich_media_allow_policy`（`--enable-rich-media-policy`）。
- 测试：`test_rich_media_service.py`（假适配器，确定性验证页码 metadata 落库；注意用唯一内容避免 `ingest_directory` 去重复用旧 blob）、`test_rich_media_api.py`（allow→服务→422）、更新两处旧断言（适配器目录语义变化）。
- 回归：pytest 102 passed + 1 skipped、Ruff、Mypy 27 源、桌面 typecheck/test/build 通过。
- 坑：共享测试 DB 里策略集持久化会导致测试互相污染（deny 规则泄漏）；策略门测试应避免插入无作用域 deny，或清理 `phase3-rich-media%` 策略集。
- 待续（Phase 3 最后）：桌面知识工作台「本地解析」入口 + 批量并发控制（GPU 占用）。

## Phase 3 桌面知识工作台入口 —— 已完成

- 后端新增 `GET /api/v1/knowledge/rich-media/pending`（`knowledge.read`），返回 `waiting_extractor` 的 PDF/图片；`rich_media_service.list_pending_rich_media`。
- 桌面 `App.tsx` 知识视图新增「待本地解析」标签页（`KnowledgePendingFile` 类型 + `loadPendingRichMedia` + `extractRichMedia` 确认弹窗 → 调 extract 端点 → 刷新）；标签页 onChange 打开时加载列表。
- 适配器目录 tab 如实显示 `status`/`execution_mode`（可用/隔离子进程 vs 等待本地运行时/未配置），不再硬编码。
- Electron `main.ts` `allowedPaths` 新增 `/api/v1/knowledge/rich-media/pending`、`/api/v1/knowledge/rich-media/extract`。
- 测试：`test_rich_media_service.py` 增 `list_pending_rich_media` 用例；`test_desktop_shell.py` 增「待本地解析/本地解析」+ 两路径断言。
- 回归：pytest 103 passed + 1 skipped、Ruff、Mypy 27 源、桌面 typecheck/test/build 通过。
- 剩余：批量并发控制（多文件抢 GPU）。
